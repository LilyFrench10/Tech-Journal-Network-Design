

Configure Static NAT 1. Define "Inside" and "Outside" on your NAT interfaces:
R1(config)#interface fastEthernet 0/0 #this connects router to network
R1(config-if)#ip nat inside
R1(config-if)#exit
R1(config)#interface serial 0/0/0 # this connects router to outside
R1(config-if)#ip nat outside
R1(config-if)#exit

R1(config)#ip nat inside source static 10.0.0.2 50.0.0.1
10.0.0.2 is the ip that will be changed, 50.0.0.1 is what it will change to


Assigning a pool rather than one static address
Create pool with
#ip nat pool test startingip# endingip# netmask 255.0.0.0

List what ips inside can access the pool 
access-list 1 permit 192.168.0.0    0.0.0.255
			              	Ips           negative subnet


R1(config)#ip nat inside source list 1 pool test overload
		ip in list 1 translate to ip in pool test
